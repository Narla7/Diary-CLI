# CLI Diary

A very simple command-line diary application written in Python. Fast and Lightweight.

## Features

- Write diary entries
- Read diary entries
- Stores entries as `.txt` files
- Entries can only be saved as the date they were entered in.

## Usage

Run:

    python main.py 

you can save the above command in your ~/.bashrc (or equivalent) to make it more efficient
