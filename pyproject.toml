[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "diary-cli"
version = "0.1.0"
description = "A simple command-line diary application"
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
Diary = "diary.main:main"
